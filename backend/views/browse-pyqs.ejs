<div class="container my-4">
    <h2 class="mb-4">
        <i class="fas fa-question-circle me-2"></i>Previous Year Questions
    </h2>
    
    <!-- Filter Section -->
    <div class="filter-section">
        <h5><i class="fas fa-filter me-2"></i>Filter PYQs</h5>
        <form method="GET" action="/pyqs">
            <div class="row">
                <div class="col-md-3 mb-3">
                    <label for="course" class="form-label">Course</label>
                    <input type="text" class="form-control" id="course" name="course" 
                           value="<%= filters.course || '' %>" placeholder="e.g., BCA, MCA">
                </div>
                <div class="col-md-3 mb-3">
                    <label for="semester" class="form-label">Semester</label>
                    <select class="form-select" id="semester" name="semester">
                        <option value="">All Semesters</option>
                        <% for(let i = 1; i <= 8; i++) { %>
                            <option value="<%= i %>" <%= filters.semester == i ? 'selected' : '' %>><%= i %></option>
                        <% } %>
                    </select>
                </div>
                <div class="col-md-3 mb-3">
                    <label for="subject" class="form-label">Subject</label>
                    <input type="text" class="form-control" id="subject" name="subject" 
                           value="<%= filters.subject || '' %>" placeholder="e.g., Math, Physics">
                </div>
                <div class="col-md-3 mb-3">
                    <label class="form-label">&nbsp;</label><br>
                    <button type="submit" class="btn btn-primary btn-custom me-2">
                        <i class="fas fa-search me-1"></i>Search
                    </button>
                    <a href="/pyqs" class="btn btn-outline-secondary btn-custom">
                        <i class="fas fa-times me-1"></i>Clear
                    </a>
                </div>
            </div>
        </form>
    </div>
    
    <!-- Results Section -->
    <% if (pyqs.length === 0) { %>
        <div class="alert alert-info alert-custom text-center">
            <i class="fas fa-info-circle me-2"></i>
            No PYQs found matching your criteria. Try adjusting your filters.
        </div>
    <% } else { %>
        <div class="row">
            <% pyqs.forEach(pyq => { %>
                <div class="col-md-6 col-lg-4 resource-card">
                    <div class="card h-100">
                        <div class="card-header bg-light">
                            <h6 class="mb-0">
                                <span class="badge bg-success me-2"><%= pyq.course %></span>
                                <span class="badge bg-secondary me-2">Sem <%= pyq.semester %></span>
                                <span class="badge bg-warning text-dark"><%= pyq.subject %></span>
                            </h6>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="fas fa-file-pdf text-danger me-2"></i>
                                <%= pyq.originalName || pyq.filename %>
                            </h5>
                            <p class="text-muted mb-3">
                                <small>
                                    <i class="fas fa-clock me-1"></i>
                                    Uploaded: <%= new Date(pyq.uploadedAt).toLocaleDateString() %>
                                </small>
                            </p>
                            
                            <div class="d-grid gap-2">
                                <a href="/viewer/pyq/<%= pyq.filename %>" 
                                   class="btn btn-success btn-custom">
                                    <i class="fas fa-eye me-2"></i>View PDF
                                </a>
                                <a href="/download/pyq/<%= pyq.filename %>" 
                                   class="btn btn-outline-success btn-custom">
                                    <i class="fas fa-download me-2"></i>Download PYQ
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } %>
</div>